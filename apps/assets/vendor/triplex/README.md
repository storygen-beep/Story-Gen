This folder will contain a minimal, vendored subset of TripleX utilities
required for the Asset Library processing pipeline (scene splitting, frame
sampling, JoyCaption2 captioning). Copied from `TripleX/` at the repo root.

No network-based caption APIs will be used.

