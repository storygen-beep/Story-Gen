# author-game eval — iteration 2 (A/B: doctrine pass vs frozen baseline)

Baseline: skill-snapshot-v2 (frozen after arousal axis, before the mopoga doctrine pass)

| set | NEW (post-pass) | BASE (pre-pass) | delta |
|---|---|---|---|
| **Regression** (cascade order — should NOT change) | 9/9 (100%) | 8/9 (89%) | no regression |
| **Doctrine** (quest-card · pitch · top-level) | 19/19 (100%) | 10/19 (53%) | **+47 pts** |
| overall | 28/28 (100%) | 18/28 (64%) | |

**Read:** the regression set is identical (both pass the objective cascade guard — the pass didn't break emission
discipline; the only nit is BASE mildly over-applied a cascade to the quiet control ambient). The doctrine set is
where the pass shows: each of the three targeted defects reproduced on BASE and was fixed on NEW —
walkthrough labels in the goal (not the tip), a desire-forward pitch that passes the heat test (BASE pitched
plot with the sex deferred), and a declared accumulation object + fail-state form + cheat skin (BASE named none).
Both configs are competent authors; the difference is precisely the doctrine, not overall quality.
