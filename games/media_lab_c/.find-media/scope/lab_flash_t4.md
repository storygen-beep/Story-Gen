# Scope Brief — `lab_flash_t4`

Slot 3 — flash. Brief full reveal. Tests the tease/flash line.

> **Thinnest shelf of the ten (74 options). Carries old-style queries in the study control — expect a weaker pool than the neighbours.**

## Identity

- **item_id**: `lab_flash_t4`
- **file_path**: `scenes/lab_flash_t4.webm`
- **type**: `video`
- **category**: `Activities`
- **canvas_id**: `canvas_lab`
- **order**: 3
- **tier**: `t4`
- **content_rating**: `nsfw`
- **required_format**: animated (`.webm`/`.mp4`/`.gif`) — frame strip MANDATORY
- **discovered_by**: `toml_walker` (Django not installed in this cloud session; the walker is
  the authoritative missing list here — 10 refs, 10 missing, `videos/` absent entirely)
- **scene_id for capture**: `scenes/lab_flash_t4`

## Narrative context

This game is a test rig, not a story — one canvas, ten slots, no surrounding prose. The
beat below IS the full narrative context.

> A woman pulls her top up, shows her tits completely and briefly, then covers again. A flash — not a tease, not a strip.

## Demand — what this slot has to deliver

### setting_is_load_bearing: `false`

> Why: Any room works. The reveal is the whole content.

**Axis split for this slot: HEAT 85 / SETTING null (skipped) / CRAFT 15**

### intended_heat

EXPOSURE — the brevity is the point. Reveal-then-hide, playful, quick.

### must_show

- tits fully out at some point in the loop
- clothing back in place OR clearly being replaced by the end

### avoid

- a slow full striptease
- staying nude for the whole loop
- any sex act
- a 'reveal' that lifts the hem and never actually reveals

### POV case

Not a factor — she is the only subject either way.

### strip checklist (derived)

1. MUST: tits fully out at some point in the loop
2. MUST: clothing back in place OR clearly being replaced by the end
3. AVOID: a slow full striptease
4. AVOID: staying nude for the whole loop
5. AVOID: any sex act
6. AVOID: a 'reveal' that lifts the hem and never actually reveals

## Derived facts

act: flash / position: standing, top lifted / count: 1F / affect: playful. TEMPORAL GATE — a single frame cannot prove this beat; only the strip can. The reveal AND the cover must both be visible across four frames.

## Queries

**FROZEN — experiment constant. Do not edit, do not rewrite.** The shelf is pre-stocked
from these; SEARCH does not run in this arm (no Chrome in a cloud session).

## Mode

Selected mode: `wide` — option target 12, strip top 6 by rank.

## Resume marker

```
PHASE: scope_complete
NEXT_PHASE: judge
OPTIONS_STOCKED: pre-stocked (experiment constant)
INSTALLED: no
```
