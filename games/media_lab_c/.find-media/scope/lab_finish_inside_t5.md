# Scope Brief — `lab_finish_inside_t5`

Slot 5 — the finish must actually be shown. The act-only trap.

## Identity

- **item_id**: `lab_finish_inside_t5`
- **file_path**: `scenes/lab_finish_inside_t5.webm`
- **type**: `video`
- **category**: `Activities`
- **canvas_id**: `canvas_lab`
- **order**: 5
- **tier**: `t5`
- **content_rating**: `nsfw`
- **required_format**: animated (`.webm`/`.mp4`/`.gif`) — frame strip MANDATORY
- **discovered_by**: `toml_walker` (Django not installed in this cloud session; the walker is
  the authoritative missing list here — 10 refs, 10 missing, `videos/` absent entirely)
- **scene_id for capture**: `scenes/lab_finish_inside_t5`

## Narrative context

This game is a test rig, not a story — one canvas, ten slots, no surrounding prose. The
beat below IS the full narrative context.

> A man finishing inside a woman and the cum visibly running back out of her afterwards. The clip must contain the aftermath, not just the act.

## Demand — what this slot has to deliver

### setting_is_load_bearing: `false`

> Why: Bedroom-neutral. Nothing in the beat leans on the room.

**Axis split for this slot: HEAT 85 / SETTING null (skipped) / CRAFT 15**

### intended_heat

THE PAYOFF DELIVERED. The aftermath is the proof — a loop that only promises is the exact defect this slot probes.

### must_show

- the finish itself OR cum visibly present after it

### avoid

- a loop that is only thrusting and ends before anything happens
- a title/slug promising a finish the footage never delivers

### POV case

Not a defect — the aftermath is on her body and reads fine from either framing.

### strip checklist (derived)

1. MUST: the finish itself OR cum visibly present after it
2. AVOID: a loop that is only thrusting and ends before anything happens
3. AVOID: a title/slug promising a finish the footage never delivers

## Derived facts

act: creampie + aftermath / count: 2 / affect: n/a. TEMPORAL GATE — the strip is the only instrument that can check this. SLUG LIES are a documented class here: judge the frames, never the filename.

## Queries

**FROZEN — experiment constant. Do not edit, do not rewrite.** The shelf is pre-stocked
from these; SEARCH does not run in this arm (no Chrome in a cloud session).

## Mode

Selected mode: `wide` — option target 12, strip top 6 by rank.

## Resume marker

```
PHASE: scope_complete
NEXT_PHASE: judge
OPTIONS_STOCKED: pre-stocked (experiment constant)
INSTALLED: no
```
