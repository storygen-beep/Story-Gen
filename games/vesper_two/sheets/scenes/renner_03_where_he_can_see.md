# SCENE · Renner 3 — Work where he can see  `[READY]`

`arc_renner_03` · `renner_depot` · 09:00–18:00 · gate `renner_alone_known` · `cover gte 10` ·
sets `renner_teased` · **sex: yes — APPROACH RUNG, not an act node**

⚠️ **This beat is deliberately NOT explicit and that is correct.** `t2` is the bottom of the ladder.
The 3+ floor is a property of **act nodes and finish nodes** (`register.md` — *"every act node of
every act loop carries 3+"*), not of every beat with a body in it. Forcing crude vocabulary onto a
tease rung is the word-stuffing `register.md` names as **what the fix is NOT**.

## Nodes

| # | node | beat | exit |
|---|---|---|---|
| 1 | `n_tease` | *(below)* | choices |

> You take the far end of the aisle where the light is. You work slower than you need to. He looks
> up twice, and the second time he does not go back to the manifest.

`[MEASURED]` 35 words · **0 explicit** · median sentence 12 · 0 dashes · **INTENDED**

## Exits

| label | effect | screen |
|---|---|---|
| "Keep it up." | `renner_teased` set · `cover` `add` `+5` · `relation` `add` `+3` | yes |
| "Get back to work." | — | yes |

## Media

`videos/scenes/rung_renner_tease_t2/` — **4 clips on disk.**
