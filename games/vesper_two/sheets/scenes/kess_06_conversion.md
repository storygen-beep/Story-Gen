# SCENE · Kess 6 — CONVERSION  `[READY]`

`arc_kess_06` · `kess_berth` · 10:00–22:00 · gate `seated_3` · sets `kess_open` · **no sex**

**The last step of the arc, and the one that turns the bench into something she can simply do.**

⚠️ **`kess_open` is the flag `bench_kess` gates on.** A milestone that opens nothing is a dead end
and gate 7 says so.

## What finishing this arc buys

1. **The bench becomes standing content** — `seated` 3 → 5, repeatable.
2. **The nightly rate starts MOVING.** `board.economy.obligation_moves` — the more he has seated,
   the more a night costs. R3b: a constant obligation against a rising income is soft by
   construction, and this one rides the sourced system instead of a second mechanic.

## Exits

| label | effect | screen |
|---|---|---|
| "Ask what else he can put in." | `kess_open` set · `relation` `add` `+3` | yes |

## Media

`videos/locations/kess_berth.jpg`.
