# SCENE · Kess 4 — It comes back wrong  `[READY]`

`arc_kess_04` · `kess_berth` · 10:00–22:00 · gate `seated_1` · sets `kess_failure` · **no sex**

## ⚠️ The only step in the game that moves a number BACKWARDS

`seated` `add` `-1`. **A staged repair that never fails is a shop counter**, and the failure is what
makes the bench a system rather than a vending machine.

## The beat

> The part has gone sour in you. He takes it back out with the pliers. You are on your back with
> the seam open. The shop light is on your naked tits and your cunt.

`[MEASURED]` 35 words · **3 explicit** · median sentence 9 — `naked` · `tits` · `cunt`.
Body words by sentence `0 0 0 3`.

## Exits

| label | effect | screen |
|---|---|---|
| "Let him take it out." | `kess_failure` set · `seated` `add` `-1` · `relation` `add` `+5` | yes |

**One exit.** She does not get a choice about a part that has already failed, and `relation` goes
*up* — the failure is the interesting problem, and the interesting problem is what he wants.

## Media

`videos/portraits/wren_naked.jpg`.
