# PLACE · The Street  `[READY]`

| | |
|---|---|
| **id** | `the_street` |
| **ENTERED FROM** | — **THIS IS THE ROOT.** Nothing is its parent, and that is the whole point (`the-map.md` R3, gate 28) |
| **WAYS OUT** | `spire_plaza` (20m) · `the_waterfront` (20m) · `underworld_strip` (20m) |
| **DOOR** | no — it is the ground, not a person's room |
| **LABELS** | `outdoors` · `public` |
| **fill** | 600 `[INTENT]` |
| **heat** | cold — one of four |

## What this place is FOR

It is the thing the other thirteen rooms sit on. The measured failure it exists to prevent: vesper
shipped **12 root locations** with `spire_plaza`, `the_waterfront` and `underworld_strip` all roots
and none of them joined, so only **9 of 35 locations were reachable on foot**. This is the one field
that fixes it.

It is also where the ascent gets a consequence surface outside anybody's room, and the only
renewable source of new characters the premise has.

## The list

| # | row | canvas | kind | system | gate | effect | screen |
|---|---|---|---|---|---|---|---|
| 1 | "Uphill, to the Spire." | `TBD` | exit | `cover` | — | `time` `add` `+20` | yes |
| 2 | "Down the dock road." | `TBD` | exit | — | — | `time` `add` `+20` | yes |
| 3 | "Under it, into the Reach." | `TBD` | exit | — | — | `time` `add` `+20` | yes |

Three rows, and all three are exits. **A ground is allowed to be a ground** — the field's median
for things-to-do-at-a-place is 3, and a corridor that declares itself a corridor is not a defect.

## Ambient

`cover` and `clean` are both read here as one swapped clause, never a branch — who looks at her
crossing, and whether they look twice. The field's differentiation is **~20 words, median 84–139
characters**: one sentence, swapped, not a big branch per state.

⚠️ No walk-in: she does no solo work here and nobody is scheduled.
