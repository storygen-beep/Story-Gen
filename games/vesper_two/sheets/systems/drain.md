# SYSTEM · drain — taking a man in order to take something off him  `[READY]`

| | |
|---|---|
| **kind** | `ambient` — fed wherever she runs it |
| **key** | `drain` (0–100) — **an ascent tier** |
| **fed at** | every drain rung across the 7 |
| **labels** | — |
| **mechanism** (S8) | a player trait, gated on `seated >= 1`. Reads are `trait` conditions and `[group]` bands. Not rotation. |

The one thing that is hers. At the bottom it is a thing she is told to do; at the top it is how she
gets in anywhere, and the reason a man above her starts being careful.

## THE LADDER

```
5  ·  10  ·  20  ·  30  ·  45  ·  60  ·  80  ·  100
```

Eight rungs, ceiling **100** with an authored gate at 100.

## READERS — written first

| # | reader | where | what changes |
|---|---|---|---|
| 1 | what a drain returns | every drain rung | bands on `drain` × `seated` — the two-halves read |
| 2 | which doors it opens | `vance_securities` · `penthouse` · `cain_lab` | reach, which is what the tier IS |
| 3 | whether a man notices afterwards | all 7 | high `drain` is not invisible any more |
| 4 | what Bastien is searching for | `bastien_backroom` | he took it once and knows where she carries it |
| 5 | what Kess will seat next | `kess_berth` | the interesting problem got more interesting |
| 6 | the quest card on the `drain` tier | guidance | S10 |

## WRITERS

Each drain rung: `drain` · `op = "add"` · `+3` to `+8` by who it was taken off.

⚠️ **Gated on a need AND a system**: `charge >= 30` and `seated >= 1`. That is the brake that is
fiction rather than mechanic — and `charge` costs 10 coin a night, so the tier is priced in the
obligation without a second lever.
