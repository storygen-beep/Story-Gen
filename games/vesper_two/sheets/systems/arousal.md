# SYSTEM · arousal — the throttle, and it is declared under protest  `[READY]`

| | |
|---|---|
| **kind** | `ambient` |
| **key** | `arousal` (0–100) |
| **fed at** | act surfaces |
| **labels** | — |
| **mechanism** (S8) | a THROTTLE — climbs in a scene, **reset to 0 at climax, author-emitted**; no engine macro does it. Not rotation. |

⚠️ **This is the deadest number in the repo and it is only declared because the loop exists.**

```
                arousal raises   arousal reads
the_allowance         25              0
seventh_day           53              0
forty_miles           52              0
steam                 55              2
back_home             47              2
                     232              4
```

`templates/board.toml` labelled the volatile layer *"NEVER gate an arc on these"* — correct about
the odometer, silent about what a throttle IS for, and five authors read it as "never gate on it at
all". In the field a sexual-state meter is a real gate in **13 of 27 games**, and where it exists it
is the **#1 or #2 most-gated thing in the whole game**.

## READERS — written first

| # | reader | where | what changes |
|---|---|---|---|
| 1 | **the act menu on a node-routed loop** | every repeatable act surface | which rungs of the act are offered |
| 2 | which finish nodes are reachable | every finish | the top of the menu |

**And nothing else.** `the-meters.md` W2: a throttle gates the repeatable act surface *and nothing
else*, and it **must never gate a one-shot capstone** — a permanent first-time beat cannot hinge on
a number that wipes.

## WRITERS

act rungs `+N` · **climax `op = "set"` · `0`** — author-emitted, every time

## The refusal condition on this sheet

⚠️ **If the node-routed act loop is not built, DELETE this system.** The cause of the 232/4 above is
structural: until the repeatable act surface existed there was nothing for a throttle to gate, so
arousal had no job. Build the loop and the throttle has one; build no loop and do not declare the
meter.
