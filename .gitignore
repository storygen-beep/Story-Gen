# ============================================================
# SECURITY — never commit
# ============================================================
*.pem
story_gen_key.pem
.env
.env.local
.env.*.local

# ============================================================
# Django / Python
# ============================================================
*.log
*.pot
*.pyc
*.pyo
*.pyd
__pycache__/
local_settings.py
db.sqlite3
db.sqlite3-*
db.sqlite3-journal
media/
staticfiles/
.Python
.python-version

# Virtualenvs
.env
.venv
venv/
ENV/
env/

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ============================================================
# Testing / type checking / lint caches
# ============================================================
.coverage
.pytest_cache/
htmlcov/
.tox/
coverage.xml
*.cover
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
test-results/
test_clips_demo_game.html

# ============================================================
# Games — SOURCE is tracked (toml_phases, books, json, output HTML).
# Only media is dropped, via the global media block below.
# ============================================================
game_books/

# ============================================================
# Media — ignored everywhere (video / images / audio).
# Skill-bundled media is re-included via !.claude/skills/** below.
# ============================================================
videos/
z_video_capture/
output.mp4
the-persistent-husband_720m.mp4
*.unknown_video

# packaged game bundles (zipped builds full of media — not source)
*.zip

# author backup snapshots (pre-edit copies — not source)
*.bak
*.bak_*

# video
*.mp4
*.webm
*.mov
*.mkv
*.m4v
*.avi
*.mpg
*.mpeg
*.flv
*.wmv

# images
*.jpg
*.jpeg
*.png
*.gif
*.webp
*.bmp
*.tif
*.tiff
*.svg
*.ico
*.avif
*.heic

# audio
*.mp3
*.wav
*.ogg
*.m4a
*.aac
*.flac

# ============================================================
# Side experiments / not part of the engine
# ============================================================
testing-remotion/
testing-canva/
testing-phaser/
testing-algorithmic-art/
testing-design-subhadra/
instagram-post-creator/

# ============================================================
# Scratch / temp data
# ============================================================
temp.json
media_analysis.json
video_*_descriptions*.json

# ============================================================
# Jupyter / notebooks
# ============================================================
.ipynb_checkpoints/
Untitled*.ipynb

# ============================================================
# IDE / editors
# ============================================================
.vscode/
.idea/
*.swp
*.swo

# ============================================================
# OS
# ============================================================
**/.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ============================================================
# Logs
# ============================================================
logs/
*.log

# ============================================================
# AI assistant state
# ============================================================
.aider*
.aider.tags.cache.*/
.claude/scheduled_tasks.lock
.claude/settings.local.json

# ============================================================
# Claude Code skills — never ignore anything inside a skill
# (skills bundle their own node_modules, lib/, etc. — partial
# tracking breaks them at runtime)
# ============================================================
!.claude/skills/
!.claude/skills/**

# ============================================================
# twine-game-explorer: browser profiles, saves, screenshots
# (binary/transient runtime output — not source)
# ============================================================
game_explorations/*/manual/profile/
game_explorations/*/profile/
game_explorations/*/screenshots/live/
game_explorations/*/archive/
game_explorations/*/.live/

# ============================================================
# Vesper — playable output media is TRACKED (the portal needs it).
# Overrides the blanket videos/ + media-extension ignores above.
# Re-include the dir first, then its contents — git cannot
# re-include a file whose parent directory is still excluded.
# Vesper is the ONLY game whose media is tracked; all other
# games' media was cleaned off disk (see games/ media policy).
# ============================================================
!games/vesper/output/videos/
!games/vesper/output/videos/**

# ============================================================
# find-media working evidence (harvest dumps, montages, candidate
# jsonl, raw transcripts) — large + transient, never source.
# ============================================================
**/.find-media/

# ============================================================
# Heavy experiments / scratch — local only, not part of the engine.
# ============================================================
test_blender/
avatar_prototype/
games/media_testbed/
game_explorations/vesper_coverplay/
game_explorations/vesper_wardrobe/

# playwright-skill temp execution files (transient)
**/.temp-execution-*.js
