---
name: author-game
description: Use when starting, continuing, or expanding an RTS-shape sandbox interactive-fiction game in this repo — e.g. "start a new game", "continue writing <game>", "add an NPC / location / arc / beat to games/<slug>", or any incremental authoring of a game's TOML rather than a one-shot generation.
---

# Author Game — interactive sequential authoring

Drives game authoring as **setup once, then continue beat-by-beat**, backed by a persistent
ledger. Replaces blind one-shot generation (whole book + whole TOML in one unsupervised pour),
which is where every structural bug hides until the 300KB end. Full design:
`docs/superpowers/specs/2026-06-03-living-plan-authoring-design.md`.

This skill is **patterns, not code.** You (the agent) do the authoring and the bookkeeping with
your normal tools (Read / Write / Edit / Bash). There is no helper module to call.

## State (per game, under `games/<slug>/`)
- `design_book.md` — intent: premise / player / economy / time model / cast + arc shapes /
  location graph / the loose end-to-end roadmap. Stable; amend explicitly.
- `authoring_state.json` — the ledger: `plan` (the living beat list) + `structure_registry`
  (what exists) + `next_up` + `decisions_log`. A data file you Read/Write directly. Shape +
  invariants: `references/ledger-schema.md`.
- `toml_phases/*.toml` — built content. `7_final_game.toml` is GENERATED by merge — never hand-edit it.

## Knowledge base (read what you need, when you need it)
- `references/ledger-schema.md` — the ledger shape + the anti-drift invariant.
- `references/setup-interview.md` — **setup mode**: the one-time interview + scaffold output.
- `references/beat-authoring.md` — **continue mode**: the per-beat loop + the doctrine self-audit.
- `references/content-design.md` — **WHAT scenes exist** (the creative-direction pass, run BEFORE
  authoring): the RTS-derived archetype catalog, the two content tracks (NPC-arc + the player/world
  lewd-activity catalog the lanes omit), the corruption **feeder economy** (the player-track supply that
  makes NPC seduction floors reachable), and the **content roster** artifact. READ at setup to build the
  roster; the beat loop fills a roster row, never improvises one.
- `references/lanes.md` — **the four lanes**: when to use each, how to write each (verbs, narrative,
  fingerprints), the hub-vs-work separation, per-arc-shape budgets, locked-visible ladder + choice-
  label vocab. READ before authoring any NPC-arc beat.
- `references/trait-design.md` — **the gating spine** (the HOW): which trait drives an arc, by shape
  (never `relation`-on-everything NOR player-`corruption`-on-everything); throttle vs odometer; capstone
  gates on the odometer not the throttle; who-initiates → primary gate; dead-meter + split-spine.
- `references/rts-design-philosophy.md` — **the WHY** behind the trait/progression model, extracted &
  verified from RTS (player-corruption-as-content-tier; odometer vs throttle; two-axis core gate;
  per-NPC odometers; **reserve the rich model for the core, peripheral arcs run light**; paced raises;
  cheap-early/expensive-deep; prose register by meter kind). Read to understand WHY before the HOW.
- `references/sex-loop.md` — **the repeatable-explicit sex-loop menu**: triggerless node-routed
  canvas, numeric state traits, climax-elect → finisher. READ before authoring repeatable explicit content.
- `references/systems.md` — **the optional-systems index**: clothing / rent / phone / customization +
  HUD — each system's TOML home, its doctrine file, and its one signature trap. READ at setup (decide
  which the game uses) + before wiring any of them.
- `references/toml-gotchas.md` — emission build-breakers (single-line inline tables, declare-traits-
  before-use, `version="1.0"` on every conditions block, locked-render, choice/stage shapes, no npc
  sidebar bars). CHECK before emitting TOML.
- `prompts_v2/COMPREHENSIVE_SYSTEM_REFERENCE.md` — ALL engine/schema/doctrine. The single
  source of truth for how to author canvases, lanes, hubs, schedules, locks, settings. Cite it.
- `prompts_v2/schema/03_example_toml.md` — the corpus's canonical worked TOML example (verbatim TLS
  Frank slice); the authoritative reference for table shapes.
- `games/late_shifts/toml_phases/` — a complete shipped game to copy concrete blocks from
  (locations, npcs, schedules, settings, canvases).

## Mode dispatch
On invocation, resolve the game slug from the request, then:
1. If `games/<slug>/authoring_state.json` does NOT exist → **setup mode**.
   Read `references/setup-interview.md` and follow it.
2. If it DOES exist → **continue mode**.
   Read `references/beat-authoring.md`; resume + reconcile first, then run the beat loop.

## Operating rules
- Ask the user ONE question at a time via the **AskUserQuestion** tool — always with concrete
  ideas, 2–4 options, and a recommendation. Never a blank prompt.
- Only offer **real** engine knobs. Every option must be grounded in
  `COMPREHENSIVE_SYSTEM_REFERENCE.md` — never invent a field/trigger the engine lacks.
- **Structure is stable-and-extensible:** it may grow (new location/NPC/flag), but ONLY via an
  explicit amendment done WHOLE (location → def + lock + schedule + unlock beat; NPC → schedule
  + open on-ramp; flag → reachable setter) and registered in `structure_registry`. Never a bare
  dangling reference. This single discipline prevents the recurring bug catalog.
- **Build NPC arcs across the four lanes** (`references/lanes.md`), sized by arc shape — never
  Lane-1-only. Keep the NPC hub (Lane 1) separate from Maya-solo work activities (Lane 3 hosts):
  if a menu choice's verb has no NPC object, it's solo work, not a hub item (§8.2 / §8.3).
- **Decide WHAT scenes exist before authoring HOW** (`references/content-design.md`). Build the
  **content roster** at setup — both tracks: NPC arcs AND the player/world lewd-activity catalog that
  feeds the player's corruption/exhibitionism odometers. A game with rich NPC arcs but no feeder
  catalog starves (the floors never clear). The beat loop fills a roster row; it never invents one cold.
- **`scope_mode` (full_game | slice) governs scale.** Set it at setup (default full_game), store it
  in the ledger, and size every arc to it + the per-NPC budget. Slice = 1 gold NPC + minimum-contract
  rest + locked-visible rungs; full_game = full budgets + the Phase 2+ Q&A.
- **Honor the per-NPC R7 brief** (voice spec + vocab ceiling + stat ladder, in `design_book.md`) when
  authoring — don't re-derive or drift it. Explicit content defaults to the **most explicit** reading
  at the NPC's declared ceiling (`doctrine/08`), never softer.
- **A Phase 2+ `include` must be mechanized** (setter/owner/threshold named) or it ships dormant —
  the validator won't catch it.
- **Validate every beat, build at milestones** (see `references/beat-authoring.md`).
